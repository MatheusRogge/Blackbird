[package]
name = "gltf"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = { workspace = true }
gltf = { version = "1.4.1", features = ["extras", "names", "import", "utils"] }

engine = { path = "../engine/" }
rendering = { path = "../rendering/" }
