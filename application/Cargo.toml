[package]
name = "application"
version = "1.0.0"
edition = "2024"

[dependencies]
pollster = "0.4.0"
engine = { path = "../engine/" }
rendering = { path = "../rendering/" }
winit = { workspace = true }
thiserror = { workspace = true }
